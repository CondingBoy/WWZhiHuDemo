<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="@dimen/list_header_height"
    android:orientation="vertical">

    <ImageView
        android:id="@+id/iv_webheader"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="centerCrop" />

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_alignParentBottom="true"
        android:background="@drawable/toppager_bg">

        <TextView
            android:id="@+id/tv_sourse"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentBottom="true"
            android:layout_alignParentRight="true"
            android:layout_marginBottom="15dp"
            android:layout_marginRight="20dp"
            android:text="图片来源"
            android:textColor="#d4d1d1"
            android:textSize="15sp" />

        <TextView
            android:id="@+id/tv_webheader_title"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_above="@id/tv_sourse"
            android:layout_marginLeft="10dp"
            android:maxLines="2"
            android:text="这是新闻头条"
            android:textColor="#fff"
            android:textSize="@dimen/pager_item_title" />
    </RelativeLayout>
</RelativeLayout>